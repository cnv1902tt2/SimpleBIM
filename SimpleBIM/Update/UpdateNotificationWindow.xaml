<Window x:Class="SimpleBIM.Update.UpdateNotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SimpleBIM - Cập Nhật Mới"
        Height="480" Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5"
        Loaded="Window_Loaded">
    <!-- ✅ REMOVED Icon attribute - sẽ set từ code-behind -->

    <Window.Resources>
        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                    <Setter Property="Foreground" Value="#666666"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D0D0D0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#0078D4" Padding="20">
            <StackPanel>
                <TextBlock Text="🎉 CẬP NHẬT MỚI CÓ SẴN!"
                           FontSize="22"
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"/>
                <TextBlock x:Name="TitleText"
                           Text="Phiên bản mới của SimpleBIM đã sẵn sàng"
                           FontSize="13"
                           Foreground="#E0E0E0"
                           Margin="0,5,0,0"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
            <StackPanel>
                <!-- Version Info -->
                <Border Background="White"
                        CornerRadius="6"
                        Padding="15"
                        Margin="0,0,0,15"
                        BorderThickness="1"
                        BorderBrush="#E0E0E0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Phiên bản hiện tại"
                                       FontSize="11"
                                       Foreground="#666666"
                                       Margin="0,0,0,5"/>
                            <TextBlock x:Name="CurrentVersionText"
                                       Text="1.0.0"
                                       FontSize="20"
                                       FontWeight="Bold"
                                       Foreground="#333333"/>
                        </StackPanel>

                        <TextBlock Grid.Column="1"
                                   Text="→"
                                   FontSize="24"
                                   FontWeight="Bold"
                                   Foreground="#0078D4"
                                   VerticalAlignment="Center"
                                   Margin="15,0"/>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Phiên bản mới nhất"
                                       FontSize="11"
                                       Foreground="#666666"
                                       Margin="0,0,0,5"/>
                            <TextBlock x:Name="LatestVersionText"
                                       Text="1.1.0"
                                       FontSize="20"
                                       FontWeight="Bold"
                                       Foreground="#0078D4"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Update Info -->
                <Border Background="White"
                        CornerRadius="6"
                        Padding="15"
                        BorderThickness="1"
                        BorderBrush="#E0E0E0">
                    <StackPanel>
                        <TextBlock Text="📋 Nội dung cập nhật:"
                                   FontSize="14"
                                   FontWeight="Bold"
                                   Foreground="#333333"
                                   Margin="0,0,0,10"/>
                        <TextBlock x:Name="ReleaseDateText"
                                   Text="Ngày phát hành: 09/12/2024"
                                   FontSize="12"
                                   Foreground="#666666"
                                   Margin="0,0,0,10"/>
                        <ScrollViewer MaxHeight="150" VerticalScrollBarVisibility="Auto">
                            <TextBlock x:Name="ReleaseNotesText"
                                       Text="- Bug fixes&#x0a;- Performance improvements"
                                       FontSize="13"
                                       Foreground="#333333"
                                       TextWrapping="Wrap"
                                       LineHeight="20"/>
                        </ScrollViewer>
                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                            <TextBlock Text="📦 Kích thước: "
                                       FontSize="12"
                                       Foreground="#666666"/>
                            <TextBlock x:Name="FileSizeText"
                                       Text="5.2 MB"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="#333333"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- ✅ NEW: Post-Download Notification -->
                <Border x:Name="PostDownloadPanel"
                        Background="#FFF9E6"
                        CornerRadius="6"
                        Padding="15"
                        BorderThickness="2"
                        BorderBrush="#FFB800"
                        Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock FontSize="14"
                                   FontWeight="Bold"
                                   Foreground="#FF8C00"
                                   Margin="0,0,0,10">
                            <Run Text="⚠️ CẬP NHẬT ĐÃ SẴN SÀNG"/>
                        </TextBlock>
                        <TextBlock TextWrapping="Wrap"
                                   FontSize="13"
                                   Foreground="#333333"
                                   LineHeight="22">
                            <Run Text="✅ File cập nhật đã được tải xuống và xác minh thành công."/>
                            <LineBreak/>
                            <LineBreak/>
                            <Run Text="📌 Để hoàn tất cập nhật, Revit cần được đóng và mở lại."/>
                            <LineBreak/>
                            <LineBreak/>
                            <Run Text="🔹 Nhấn " FontWeight="Bold"/>
                            <Run Text="&quot;Đóng Revit &amp; Cập Nhật&quot;" Foreground="#0078D4" FontWeight="Bold"/>
                            <Run Text=" để tự động đóng Revit và cài đặt."/>
                            <LineBreak/>
                            <Run Text="🔹 Nhấn " FontWeight="Bold"/>
                            <Run Text="&quot;Cập Nhật Sau&quot;" Foreground="#666666" FontWeight="Bold"/>
                            <Run Text=" để hủy và xóa file tải xuống."/>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Progress Bar (Hidden by default) -->
        <Border Grid.Row="2"
                x:Name="ProgressPanel"
                Background="White"
                Padding="20,15"
                BorderBrush="#E0E0E0"
                BorderThickness="0,1,0,0"
                Visibility="Collapsed">
            <StackPanel>
                <TextBlock x:Name="ProgressText"
                           Text="Đang tải xuống..."
                           FontSize="13"
                           Foreground="#333333"
                           Margin="0,0,0,8"/>
                <ProgressBar x:Name="ProgressBar"
                             Height="8"
                             Value="0"
                             Minimum="0"
                             Maximum="100"
                             Background="#E0E0E0"
                             Foreground="#0078D4"/>
            </StackPanel>
        </Border>

        <!-- Action Buttons -->
        <Border Grid.Row="3" Background="White" Padding="20" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- ✅ Pre-Download Buttons (visible before download) -->
                <StackPanel x:Name="PreDownloadButtons" 
                            Grid.Column="0" 
                            Grid.ColumnSpan="3"
                            Orientation="Horizontal"
                            HorizontalAlignment="Center">
                    <Button x:Name="SkipButton"
                            Content="Bỏ Qua Phiên Bản Này"
                            Style="{StaticResource SecondaryButton}"
                            Click="SkipButton_Click"
                            Padding="15,10"
                            Margin="0,0,10,0"/>

                    <Button x:Name="RemindLaterButton"
                            Content="Nhắc Lại Sau"
                            Style="{StaticResource SecondaryButton}"
                            Click="RemindLaterButton_Click"
                            Margin="0,0,10,0"
                            Padding="15,10"/>

                    <Button x:Name="UpdateNowButton"
                            Content="Cập Nhật Ngay"
                            Style="{StaticResource ModernButton}"
                            Click="UpdateNowButton_Click"
                            Padding="25,10"/>
                </StackPanel>

                <!-- ✅ Post-Download Buttons (visible after download completes) -->
                <StackPanel x:Name="PostDownloadButtons" 
                            Grid.Column="0" 
                            Grid.ColumnSpan="4"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Visibility="Collapsed">
                    <Button x:Name="UpdateLaterButton"
                            Content="🗑️ Cập Nhật Sau (Xóa File)"
                            Style="{StaticResource SecondaryButton}"
                            Click="UpdateLaterButton_Click"
                            Padding="15,10"
                            Margin="0,0,10,0"/>

                    <Button x:Name="CloseRevitButton"
                            Content="⚡ Đóng Revit &amp; Cập Nhật"
                            Style="{StaticResource ModernButton}"
                            Click="CloseRevitButton_Click"
                            Padding="25,10"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>